# SSH Config Example
# File: ~/.ssh/config
# Usage: $ ssh foobar

# SSH Identities:
# $ ssh-keygen -f ~/.ssh/<identity> -C "<identity>"
# $ ssh-add ~/.ssh/<identity>
# $ ssh-add -l

Host foobar
Compression yes
HostName 127.0.0.1
User username
Port 22